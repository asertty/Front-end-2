# 이론 수업
| 자바 스크립트로 백앤드와 프론트앤드에서 자주 사용하는 프로그램

### 자바 스크립트 

    주석, 함수, 변수 등을 다시 한번 보았다.
    var는 여러번 선언할 수 있고, let는 변수를 한번만 선언 할 수 있다.
    null은 인위적으로 비어있는것을 뜻하고,
    undefined은 잘 모르는것 뜻한다.
    계산 식은 괄호가 먼저이고 그 다음 --,++ -> +++ 등 처럼 순서가 있는데
    그리고 삼항연산자에 대해서 한번 예제를 풀어보고
    반복문으로 if문이랑 do while문을 다시 한번 들었다.


#### 오늘 한 코드

    <!DOCTYPE html>
    <html lang="ko">
    <head>
        <title>Document</title>
    </head>
    <body>
        <script>
    
            let 제목 = "<h1>이것은 제목입니다.</h1>";
            제목 = 제목 + "<h1>ㅋㅋㅋㅋ</h1>";
            document.write(제목);
    
            let 결과 = (10 >= 8);
            document.write(결과);
    
            // 삼항연산자
            
            let 숫자1 = 18;
            let 숫자2 = 28;
            alert(숫자1 > 숫자2 ? "참" : "거짓");
            let result = 숫자1 > 숫자2 ? "참" : "거짓";
            console.log (result); 
            
            //구구단 만들기
    
            let 단;
            
            while(true){
                
                단 = window.prompt("입력하시오.");
                // 단이 0이면 멈춘다
    
                if(단 ==0){
                    break
                }
    
                document.write("<h2>"+단+"단</h2>");
                //  초기값; 조건식; 증감식
                for(let a = 1; a<=9; a++){
                    document.write(단+'*'+a+'='+(단+a)+'<br>');
                };
    
            }
    
        </script>
    </body>
    </html>
